/**
 * Raw hex for the site's accent color, for contexts that can't use the `gold`
 * Tailwind token — SVG `stroke`/`fill` attributes and inline styles, which need
 * an actual color value rather than a class name.
 */
export const GOLD_HEX = '#8FA89B'
